@page
@model IndexModel
@{
    ViewData["Title"] = "Home";
}

            <div style="text-align: center;">
                <img src="BASF.png" height=414px; />
                <h1>Verifiable Credential Issuance and Verifier Demo</h1>
        
                <button type="button" id="idIssue" class="button">GET CREDENTIAL</button>
                <button type="button" id="idVerify" class="button">VERIFY CREDENTIAL</button><br><br><br>
                <button type="button" id="idVerify" class="button">ONBOARDING VC DEMO</button>
                <script>
                        var idIssue = document.getElementById('idIssue');
                        var idVerify = document.getElementById('idVerify');
                        var idOnboarding = document.getElementById('idOnboarding');

                        idIssue.addEventListener('click', () => {
                            window.location = "issuer";
                        })
                        idVerify.addEventListener('click', () => {
                            window.location = "verifier";
                        })
                        idVerify.addEventListener('click', () => {
                            window.location = "Onboarding";
                    </script>
            </div>
